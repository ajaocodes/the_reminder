<!DOCTYPE html>
<html lang="en">

<!-- THE HEAD TAG PARTIAL -->
<%- include("../partials/head") %>

<body>
      <!-- THE HEADER TAG PARTIAL -->
      <%- include("../partials/header") %>

      <a href ="/reminder/new"><button>Add a Reminder</button></a>

      <a href="/auth/logout"><button class="button">Logout</button></a>

      

    <h1>Welcome to the Reminder App</h1><br>
      <ul>

        See all reminders below:

        <ul>
          <% for (let i=0; i < reminder.length; i++) { %>
            <%= console.log(reminder[i]._id) %>
                <a href="/reminder/<%= reminder[i]._id %>"><%= reminder[i].title %></a> <br>

              <form action="/reminder/<%= reminder._id %>?_method=delete" method="post">
                <input class="button" type="submit" value="Delete Reminder" />
            </form>
           <% } %>
          </ul>
     
    </ul>

      <!-- THE HEAD TAG PARTIAL -->
      <%- include("../partials/footer") %>
</body>
</html>